<div class="nav-scroller py-1 mb-2">
    <nav class="nav d-flex justify-content-between">
        <a class="p-2 text-muted" href="{{ route('mainPage') }}">Главная</a>
        <a class="p-2 text-muted" href="{{ route('aboutPage') }}">О нас</a>
        <a class="p-2 text-muted" href="{{ route('createArticle') }}">Создать статью</a>
        <a class="p-2 text-muted" href="{{ route('contactsPage') }}">Контакты</a>
        <a class="p-2 text-muted" href="{{ route('feedbackPage') }}">Список обращений</a>
        <a class="p-2 text-muted" href="#">Админ. раздел</a>
    </nav>
</div>
